# Environnement complet (backend + frontend) : c'est ce que la CI et le README
# installent. Volontairement une agrégation et non un `pip freeze` : le freeze
# précédent avait divergé - il lui manquait weasyprint, staticmap et
# fontawesomefree, ajoutés pour le carnet PDF, si bien que la CI testait un jeu
# de dépendances différent de celui des images Docker.
#
# Les Dockerfiles installent chacun son fichier de service ; ces deux-là restent
# donc la seule source de vérité, et les versions y sont épinglées.
# Les outils de test (pytest, ruff) sont à part, dans requirements.dev.txt.
-r requirements.backend.txt
-r requirements.frontend.txt
